[buildout]
extends =
    base.cfg
    https://raw.github.com/collective/buildout.plonetest/master/qa.cfg

eggs +=
    plone.reload
    Products.PDBDebugMode
    ipdb

extensions +=
    mr.developer

parts +=
    omelette
    translation
    test
    robot

extensions +=
    mr.developer

sources = sources
auto-checkout +=
    collective.behavior.talcondition
    collective.excelexport

[sources]
collective.behavior.talcondition = git https://github.com/IMIO/collective.behavior.talcondition
collective.excelexport = git git@github.com:collective/collective.excelexport.git

[omelette]
recipe = collective.recipe.omelette
eggs = ${test:eggs}

[translation]
recipe = infrae.i18nextract
packages = collective.documentgenerator
output = ${buildout:directory}/src/collective/documentgenerator/locales
output-package = collective.documentgenerator
domain = collective.documentgenerator

[test]
recipe = zc.recipe.testrunner
eggs =
    ${buildout:eggs}
    ${buildout:package-name} ${buildout:package-extras}
defaults = ['-s', '${buildout:package-name}', '--auto-color', '--auto-progress']

[robot]
recipe = zc.recipe.egg
eggs =
    Pillow
    ${test:eggs}
    plone.app.robotframework[ride,reload]
